IMAGE=plainsightai/openfilter-dev:dev
SOURCE=file:///videos/recording.mp4!loop!resize=1280x720!maxfps=10
VIDEO_DIR=/tmp
WEB_PORT=8000
TZ=UTC  # make env overwrites this with the host's zone
TIMINGS=bottom-left  # cameras usually burn their own clock at the top
